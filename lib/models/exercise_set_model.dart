class ExerciseSet {
  int reps;
  double weight;
  bool userModified;
  bool isCompleted;

  ExerciseSet(
      {this.reps = 0,
      this.weight = 0.0,
      this.userModified = false,
      this.isCompleted = false});
}
