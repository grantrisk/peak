{
  "scripts": {
    "format": "dart format . && npx prettier --write .",
    "build_mocks": "flutter pub run build_runner build --delete-conflicting-outputs",
    "tests": "npm run build_mocks && flutter test"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
