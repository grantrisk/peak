{
  "scripts": {
    "format": "dart format . && npx prettier --write .",
    "tests": "flutter test"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
